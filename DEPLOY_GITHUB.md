# 🚀 Деплой на GitHub Pages

## Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com/new
2. Название: `kb-sits`
3. Описание: `Система бронирования мест в коворкинге`
4. Public
5. Нажмите **Create repository**

## Шаг 2: Инициализируйте Git локально

```bash
cd "/Users/admin/Desktop/ КБ Ситс"

# Инициализация
git init

# Добавить все файлы
git add .

# Первый коммит
git commit -m "Initial commit: КБ Ситс с Supabase"

# Добавить remote
git remote add origin https://github.com/ВАШ_USERNAME/kb-sits.git

# Переименовать ветку в main
git branch -M main

# Отправить на GitHub
git push -u origin main
```

## Шаг 3: Настройте GitHub Pages

1. Откройте репозиторий на GitHub
2. Settings → Pages
3. Source: **Deploy from a branch**
4. Branch: **main** → **/ (root)**
5. Нажмите **Save**

Через 1-2 минуты сайт будет доступен по адресу:
```
https://ВАШ_USERNAME.github.io/kb-sits/
```

## ⚠️ ВАЖНО: Настройка для Supabase

GitHub Pages работает только с **HTTPS**, поэтому нужно:

### Вариант 1: Использовать Supabase Cloud (рекомендуется)

1. Создайте проект на https://supabase.com
2. Примените SQL схему из `supabase-schema.sql`
3. Обновите `config.js`:

```javascript
const SUPABASE_CONFIG = {
  url: 'https://ВАШ_ПРОЕКТ.supabase.co',
  anonKey: 'ВАШ_КЛЮЧ_ИЗ_SUPABASE_CLOUD'
};
```

4. Закоммитьте и запушьте:
```bash
git add config.js
git commit -m "Update Supabase config for production"
git push
```

### Вариант 2: Локальный Supabase (только для разработки)

Локальный Supabase (`http://127.0.0.1:54321`) не будет работать на GitHub Pages.
Используйте его только для локальной разработки.

## 📝 .gitignore

Убедитесь, что у вас есть `.gitignore`:

```
# Локальные файлы
.DS_Store
*.log

# Не коммитить чувствительные данные
# config.js  # Раскомментируйте, если не хотите публиковать ключи
```

## 🔄 Обновление сайта

После изменений:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Сайт обновится автоматически через 1-2 минуты.

## 🔒 Безопасность

⚠️ **НЕ коммитьте:**
- Приватные ключи
- Пароли
- service_role ключи Supabase

✅ **Можно коммитить:**
- anon (public) ключ Supabase
- URL Supabase проекта

## 📦 Структура для деплоя

Убедитесь, что в корне есть:
- ✅ `index.html`
- ✅ `style.css`
- ✅ `app.js`
- ✅ `config.js`
- ✅ `supabase-api.js`
- ✅ `migration.js`
- ✅ `README.md`

## 🧪 Проверка после деплоя

1. Откройте `https://ВАШ_USERNAME.github.io/kb-sits/`
2. Откройте консоль (F12)
3. Проверьте подключение:

```javascript
console.log('Supabase:', typeof supabase);
const { data } = await api.client.from('departments').select('*');
console.log('Data:', data);
```

## 🆘 Проблемы?

### Сайт не открывается
- Подождите 2-3 минуты после первого деплоя
- Проверьте Settings → Pages

### Ошибка CORS
- Убедитесь, что используете HTTPS URL Supabase
- Проверьте настройки CORS в Supabase Dashboard

### Ошибка "Failed to fetch"
- Проверьте, что Supabase URL правильный
- Проверьте, что anon key актуальный

## 📚 Полезные ссылки

- GitHub Pages: https://pages.github.com/
- Supabase Cloud: https://supabase.com
- Документация: см. README.md в проекте
