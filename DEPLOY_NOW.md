# ✅ Готово к деплою!

## Что уже сделано:

1. ✅ Git репозиторий инициализирован
2. ✅ Все файлы добавлены
3. ✅ Первый коммит создан
4. ✅ .gitignore настроен

## 🚀 Следующие шаги:

### 1. Создайте репозиторий на GitHub

Откройте: https://github.com/new

- **Repository name**: `kb-sits`
- **Description**: `Система бронирования мест в коворкинге`
- **Public** ✓
- Нажмите **Create repository**

### 2. Подключите remote и запушьте

Скопируйте URL вашего репозитория (например: `https://github.com/username/kb-sits.git`)

Затем выполните:

```bash
cd "/Users/admin/Desktop/ КБ Ситс"

# Добавить remote (замените username на ваш)
git remote add origin https://github.com/username/kb-sits.git

# Переименовать ветку в main
git branch -M main

# Отправить на GitHub
git push -u origin main
```

### 3. Настройте GitHub Pages

1. Откройте ваш репозиторий на GitHub
2. **Settings** → **Pages**
3. **Source**: Deploy from a branch
4. **Branch**: main → / (root)
5. Нажмите **Save**

Через 1-2 минуты сайт будет доступен:
```
https://username.github.io/kb-sits/
```

## ⚠️ ВАЖНО: Supabase для production

GitHub Pages работает только с HTTPS. Локальный Supabase не будет работать.

### Вариант 1: Supabase Cloud (рекомендуется)

1. Создайте проект на https://supabase.com
2. SQL Editor → вставьте `supabase-schema.sql` → Run
3. Settings → API → скопируйте URL и anon key
4. Обновите `config.js`:

```javascript
const SUPABASE_CONFIG = {
  url: 'https://ваш-проект.supabase.co',
  anonKey: 'ваш-ключ'
};
```

5. Закоммитьте:
```bash
git add config.js
git commit -m "Update config for production"
git push
```

### Вариант 2: Только frontend (без БД)

Если хотите задеплоить только frontend без Supabase:

1. Удалите подключение Supabase из `index.html`:
```html
<!-- Закомментируйте эти строки -->
<!-- <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script> -->
<!-- <script src="config.js"></script> -->
<!-- <script src="supabase-api.js"></script> -->
```

2. Приложение будет работать с localStorage

## 📝 Обновление сайта

После любых изменений:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Сайт обновится автоматически через 1-2 минуты.

## 🧪 Проверка после деплоя

1. Откройте ваш сайт
2. F12 → Console
3. Проверьте:

```javascript
// Если используете Supabase
const { data } = await api.client.from('departments').select('*');
console.log(data);

// Если localStorage
console.log(localStorage.getItem('users'));
```

## 📚 Документация

Все инструкции в проекте:
- **DEPLOY_GITHUB.md** - детальный деплой
- **QUICKSTART.md** - быстрый старт с Supabase
- **README.md** - описание проекта

## 🎉 Готово!

После выполнения шагов ваш сайт будет доступен по адресу:
```
https://ваш-username.github.io/kb-sits/
```

Удачи! 🚀
