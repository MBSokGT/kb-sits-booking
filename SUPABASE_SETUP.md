# 🗄️ Настройка Supabase БД

## Шаг 1: Создание проекта

1. Зайдите на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Дождитесь инициализации (~2 минуты)

## Шаг 2: Выполнение SQL схемы

1. Откройте **SQL Editor** в левом меню
2. Нажмите **New Query**
3. Скопируйте весь код из `supabase-schema.sql`
4. Вставьте в редактор
5. Нажмите **Run** (или Ctrl+Enter)

## Шаг 3: Включение Realtime

1. Откройте **Database** → **Replication**
2. Найдите таблицы:
   - `bookings`
   - `zones`
   - `floors`
   - `seats`
3. Включите переключатель **Realtime** для каждой

## Шаг 4: Получение ключей

1. Откройте **Settings** → **API**
2. Скопируйте:
   - **Project URL** (например: `https://xxx.supabase.co`)
   - **anon public** ключ

## Шаг 5: Настройка приложения

Откройте `config.js` и вставьте ваши ключи:

```javascript
const SUPABASE_CONFIG = {
  url: 'https://ваш-проект.supabase.co',
  anonKey: 'ваш-anon-key'
};
```

## Готово! 🎉

Теперь:
- Откройте `index.html` в браузере
- Войдите как `admin@demo.ru` / `admin123`
- Все данные хранятся в Supabase
- Real-time синхронизация работает

## Проверка работы

Откройте 2 браузера:
1. В первом забронируйте место
2. Во втором сразу увидите изменения

## Возможные проблемы

**Ошибка: "relation already exists"**
- Решение: Удалите все таблицы вручную и запустите схему заново

**Ошибка: "already member of publication"**
- Решение: Схема сама проверяет и не добавляет дубликаты

**Не работает вход**
- Проверьте что демо пользователи созданы: `SELECT * FROM users;`
- Пароли хранятся в открытом виде для демо (в продакшене используйте bcrypt)
